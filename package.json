{
  "name": "md-links",
  "version": "0.1.0",
  "description": "Read and verificade all the links in your directory",

  "scripts": {
    "eslint": "eslint --ext .js src/ test/",
    "pretest": "npm run eslint",
    "test": "jest --coverage",
    "install": "node validatePath.js"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "jest": "^24.7.1"
  },
  "dependencies": {
    "eslint-plugin-jest": "^22.4.1"
  }
}
